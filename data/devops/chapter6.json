{
  "title": "Chương 6: GitHub Actions — CI/CD tích hợp trong GitHub",
  "theory": [
    {
      "heading": "CI/CD là gì? Lợi ích",
      "content": "**Continuous Integration (CI):**\n- Tự động integrate và test code changes thường xuyên\n- Developer commit code lên **shared repository**\n- **Automated tests** chạy để đảm bảo không break existing functionality\n\n**Continuous Deployment (CD):**\n- Tự động hóa việc **release code changes**\n- Sau khi code pass tests và được approve → tự động deploy lên production/staging\n- Không cần can thiệp thủ công\n\n**Key Benefits:**\n- **Faster Delivery** — tự động hóa testing và deployment\n- **Higher Code Quality** — automated tests bắt lỗi sớm\n- **Reduced Errors** — ít manual intervention\n- **Continuous Feedback** — developer phát hiện bugs nhanh"
    },
    {
      "heading": "GitHub Actions là gì? Key Features",
      "content": "**GitHub Actions** là **CI/CD tool tích hợp sẵn trong GitHub**.\n- Cho phép tạo custom workflows để build, test, deploy code trực tiếp từ GitHub repository\n- Workflows được định nghĩa bằng **YAML files** bên trong repository\n\n**Key Features:**\n- **Automation of workflows** — build, test, deploy tự động\n- **Event-driven workflows** — trigger theo events: `push`, `pull_request`, `release`, `schedule`\n- **Support for multiple languages** — JavaScript, Python, Go, Java...\n- **Custom actions** — reusable code có thể share qua workflows hoặc repositories khác nhau"
    },
    {
      "heading": "Key Concepts: Workflow, Events, Jobs, Steps, Runners",
      "content": "**Workflow:**\n- Automated process định nghĩa bằng **YAML file**\n- Chạy 1 hoặc nhiều jobs\n- Trigger bởi events hoặc schedule\n- Location: `.github/workflows/` directory\n\n**Events:**\n- Activities trigger workflow: `push`, `pull_request`, `release`, scheduled cron jobs\n\n**Jobs:**\n- Tập hợp steps chạy trên cùng **runner**\n- Có thể chạy **sequentially** hoặc **in parallel**\n- Mỗi job chạy trên own virtual machine (runner)\n\n**Steps:**\n- Individual tasks làm nên một job\n- Có thể: run commands, run scripts, hoặc gọi **pre-built actions**\n\n**Runners:**\n- Servers thực thi jobs\n- GitHub cung cấp **free hosted runners**: `ubuntu-latest`, Windows, macOS\n- Có thể dùng **self-hosted runners**\n- Mỗi hosted runner có predefined environment"
    },
    {
      "heading": "Anatomy of a GitHub Actions Workflow — YAML Structure",
      "content": "**Cấu trúc workflow YAML:**\n- `name` — tên workflow\n- `on` — triggers (`push`/`pull_request` với branches filter)\n- `jobs` — chứa các jobs\n\n**Trong mỗi job:**\n- `runs-on` — chỉ định runner environment (ví dụ: `ubuntu-latest`)\n- `steps` — các bước thực hiện\n\n**Ví dụ CI workflow cho Spring Boot Maven:**\n1) **Checkout repository** — dùng `actions/checkout@v2` (pull code từ repo)\n2) **Set up JDK** — dùng `actions/setup-java@v4` với `java-version: 17`\n3) **Cache Maven repository** — dùng `actions/cache@v4`\n- `path`: `~/.m2/repository`\n- `key`: dựa trên `runner.os` + `hashFiles('**/pom.xml')` (tự invalidate khi pom.xml thay đổi)\n4) **Build with Maven** — chạy `mvn clean package -DskipTests`\n5) **Run tests** — chạy `mvn test`"
    },
    {
      "heading": "GitHub Secrets — Quản lý thông tin nhạy cảm",
      "content": "**Secrets** là biến môi trường được **mã hóa** lưu trong GitHub repository.\n\n**Truy cập trong workflow:**\n- Cú pháp: `${{ secrets.SECRET_NAME }}`\n- Ví dụ: `${{ secrets.DOCKER_PASSWORD }}`, `${{ secrets.SLACK_WEBHOOK_URL }}`, `${{ secrets.HEROKU_API_KEY }}`\n\n**Cách tạo Secret:**\n1) Vào repository **Settings > Secrets and variables > Actions**\n2) Click **'New repository secret'**\n3) Nhập tên và giá trị\n\n⚠️ NOTE: KHÔNG bao giờ hardcode credentials trực tiếp trong workflow file — luôn dùng secrets."
    },
    {
      "heading": "Advanced Features: Matrix Strategy, Caching, Notifications",
      "content": "**1) Parallel Jobs / Matrix Strategy:**\n- Chạy nhiều jobs song song trên nhiều environments\n- Ví dụ: test trên JDK 8, 11, 17 cùng lúc\n- Cú pháp: `strategy: { matrix: { java-version: [8, 11, 17] } }`\n\n**2) Caching Dependencies:**\n- Dùng `actions/cache@v4` để cache `~/.m2/repository` (Maven)\n- `key` dựa trên `runner.os` + `hashFiles('**/pom.xml')`\n- Cache tự **invalidate** khi `pom.xml` thay đổi\n\n**3) Notifications:**\n- Gửi Slack notification sau build\n- Dùng action `8398a7/action-slack@v3`\n- Cần `SLACK_WEBHOOK_URL` từ secrets\n\n**4) `needs` keyword:**\n- Khai báo **dependency giữa jobs**\n- Job B chỉ chạy sau khi Job A hoàn thành thành công"
    },
    {
      "heading": "Continuous Deployment với GitHub Actions",
      "content": "Sau khi CI pass, setup CD để **auto-deploy**.\n\n**Deploy to Docker Hub:**\n1) `docker login` với `${{ secrets.DOCKER_PASSWORD }}`\n2) `docker build -t username/app .`\n3) `docker push username/app`\n\n**Deploy to Heroku:**\n- Dùng action `akhileshns/heroku-deploy@v3`\n- Cần: `heroku_api_key`, `heroku_app_name`\n\n**Deploy to AWS Elastic Beanstalk:**\n1) `eb init -p java-17 app-name --region us-west-2`\n2) `eb deploy`\n\n**Multi-stage Dockerfile:**\n- **Stage 1 (build):** dùng `maven:3.9.7` — compile và tạo JAR\n- **Stage 2 (runtime):** dùng `openjdk:21` — chỉ copy JAR từ stage 1\n- Lợi ích: **image nhỏ gọn hơn** — không chứa Maven, source code, hay build tools"
    }
  ],
  "questions": [
    {
      "id": 1,
      "level": "basic",
      "question": "GitHub Actions workflow được định nghĩa bằng file loại nào?",
      "options": ["JSON files trong .github/actions/", "YAML files trong .github/workflows/", "XML files trong src/ci/", "Groovy files trong root directory"],
      "answer": 1,
      "explanation": "GitHub Actions workflows được định nghĩa bằng YAML files đặt trong thư mục .github/workflows/ của repository."
    },
    {
      "id": 2,
      "level": "basic",
      "question": "Trong GitHub Actions, 'Runner' là gì?",
      "options": ["Tên của một workflow", "Server thực thi các jobs", "Người quản trị repository", "Một loại trigger event"],
      "answer": 1,
      "explanation": "Runner là server thực thi các jobs trong GitHub Actions. GitHub cung cấp free hosted runners (ubuntu-latest, windows-latest, macos-latest) hoặc bạn có thể dùng self-hosted runners."
    },
    {
      "id": 3,
      "level": "basic",
      "question": "Keyword 'on' trong GitHub Actions workflow dùng để làm gì?",
      "options": ["Bật/tắt workflow", "Định nghĩa events trigger workflow (push, pull_request, schedule...)", "Khai báo tên workflow", "Chỉ định runner environment"],
      "answer": 1,
      "explanation": "Keyword 'on' định nghĩa triggers — events nào sẽ kích hoạt workflow. Ví dụ: on: push: branches: [master] sẽ chạy khi có push lên branch master."
    },
    {
      "id": 4,
      "level": "basic",
      "question": "Action 'actions/checkout@v2' trong GitHub Actions được dùng để làm gì?",
      "options": ["Deploy code lên server", "Pull source code từ repository về runner", "Setup JDK version", "Chạy unit tests"],
      "answer": 1,
      "explanation": "actions/checkout là pre-built action pull source code từ GitHub repository về runner để các steps tiếp theo có thể làm việc với code."
    },
    {
      "id": 5,
      "level": "basic",
      "question": "GitHub Secrets được truy cập trong workflow bằng cú pháp nào?",
      "options": ["@secrets.SECRET_NAME", "${{ secrets.SECRET_NAME }}", "env.SECRET_NAME", "config.SECRET_NAME"],
      "answer": 1,
      "explanation": "Trong GitHub Actions, secrets được truy cập bằng expression syntax: ${{ secrets.SECRET_NAME }}. Ví dụ: ${{ secrets.DOCKER_PASSWORD }}, ${{ secrets.SLACK_WEBHOOK_URL }}."
    },
    {
      "id": 6,
      "level": "basic",
      "question": "Trong GitHub Actions, Jobs có thể chạy theo cách nào?",
      "options": ["Chỉ chạy tuần tự, không thể song song", "Chỉ chạy song song, không thể tuần tự", "Sequentially (tuần tự) hoặc in parallel (song song)", "Luôn chạy theo thứ tự alphabet"],
      "answer": 2,
      "explanation": "Jobs có thể chạy sequentially (dùng 'needs' keyword để tạo dependency) hoặc in parallel (mặc định, không có needs). Mỗi job chạy trên own virtual machine."
    },
    {
      "id": 7,
      "level": "basic",
      "question": "Key benefit của CI/CD là gì?",
      "options": ["Tăng số lượng developer cần thiết", "Faster Delivery, Higher Code Quality, Reduced Errors, Continuous Feedback", "Loại bỏ hoàn toàn nhu cầu testing", "Chỉ áp dụng được cho Java projects"],
      "answer": 1,
      "explanation": "CI/CD mang lại: Faster Delivery (tự động hóa), Higher Code Quality (automated tests), Reduced Errors (ít manual intervention), Continuous Feedback (phát hiện bugs sớm)."
    },
    {
      "id": 8,
      "level": "intermediate",
      "question": "Action 'actions/cache@v4' trong GitHub Actions được dùng để làm gì?",
      "options": ["Xóa cache sau mỗi build", "Cache dependencies (Maven .m2, npm node_modules) để tăng tốc build", "Lưu artifacts sau khi build", "Cache Docker images"],
      "answer": 1,
      "explanation": "actions/cache@v4 cache thư mục dependencies (ví dụ: ~/.m2/repository cho Maven). Cache key thường dựa trên runner.os + hash của pom.xml — khi pom.xml thay đổi, cache tự invalidate và download dependencies mới."
    },
    {
      "id": 9,
      "level": "intermediate",
      "question": "Matrix strategy trong GitHub Actions dùng để làm gì?",
      "options": ["Chạy job trên nhiều environments/versions song song — ví dụ test trên JDK 8, 11, 17 cùng lúc", "Định nghĩa ma trận bảo mật cho repository", "Cấu hình multiple runners", "Tạo nhiều workflows từ một template"],
      "answer": 0,
      "explanation": "Matrix strategy: strategy: { matrix: { java-version: [8, 11, 17] } } — GitHub Actions tạo 3 job instances chạy song song, mỗi job test với một JDK version khác nhau. Rất hiệu quả để đảm bảo compatibility."
    },
    {
      "id": 10,
      "level": "intermediate",
      "question": "Keyword 'needs' trong GitHub Actions job definition có nghĩa gì?",
      "options": ["Job cần thêm resources", "Job chỉ chạy sau khi job được chỉ định hoàn thành thành công", "Job cần có secrets", "Job yêu cầu self-hosted runner"],
      "answer": 1,
      "explanation": "needs: [job-name] tạo dependency giữa jobs — job hiện tại chỉ chạy sau khi job được chỉ định hoàn thành. Ví dụ: deploy job cần build job chạy xong trước."
    },
    {
      "id": 11,
      "level": "intermediate",
      "question": "Trong workflow YAML, 'runs-on: ubuntu-latest' có nghĩa gì?",
      "options": ["Workflow chỉ chạy khi push lên Ubuntu", "Job sẽ chạy trên GitHub-hosted runner với môi trường Ubuntu mới nhất", "Cần cài Ubuntu trên máy developer", "Job chạy trong Ubuntu Docker container local"],
      "answer": 1,
      "explanation": "runs-on chỉ định runner environment. ubuntu-latest = GitHub-hosted runner với Ubuntu mới nhất. GitHub cũng cung cấp windows-latest và macos-latest."
    },
    {
      "id": 12,
      "level": "intermediate",
      "question": "Để deploy lên Docker Hub trong GitHub Actions, cần làm gì đầu tiên?",
      "options": ["Cài Docker Desktop trên runner", "Login vào Docker Hub dùng secrets: echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin", "Tạo Dockerfile trong .github/workflows/", "Push code trực tiếp không cần login"],
      "answer": 1,
      "explanation": "Step đầu tiên là docker login với credentials từ secrets. Dùng --password-stdin để tránh password xuất hiện trong logs. Sau đó docker build và docker push."
    },
    {
      "id": 13,
      "level": "intermediate",
      "question": "Sự khác biệt giữa 'run' và 'uses' trong một step của GitHub Actions là gì?",
      "options": ["run chạy shell commands; uses gọi pre-built actions từ marketplace", "run chạy trên Linux; uses chạy trên Windows", "run dùng cho CI; uses dùng cho CD", "Không có sự khác biệt"],
      "answer": 0,
      "explanation": "run: thực thi shell command trực tiếp (ví dụ: run: mvn test). uses: gọi một pre-built action từ GitHub marketplace hoặc repository khác (ví dụ: uses: actions/checkout@v2)."
    },
    {
      "id": 14,
      "level": "advanced",
      "question": "Trong CI workflow cho Spring Boot, tại sao dùng 'mvn clean package -DskipTests' thay vì 'mvn clean package'?",
      "options": [
        "Để build nhanh hơn bằng cách bỏ qua tests ở bước build, tests sẽ được chạy riêng ở step 'Run tests'",
        "Vì GitHub Actions không hỗ trợ chạy tests",
        "Để tạo WAR file thay vì JAR file",
        "-DskipTests là bắt buộc khi dùng Maven trên Ubuntu"
      ],
      "answer": 0,
      "explanation": "-DskipTests bỏ qua việc chạy tests trong bước build (chỉ compile). Tests được chạy riêng ở step kế tiếp (mvn test) để tách biệt build và test phases, dễ debug hơn khi có lỗi."
    },
    {
      "id": 15,
      "level": "advanced",
      "question": "Multi-stage Dockerfile (FROM maven AS build + FROM openjdk) mang lại lợi ích gì?",
      "options": [
        "Cho phép dùng nhiều base images, giai đoạn build dùng Maven image (nặng), runtime image chỉ cần openjdk (nhỏ gọn) — giảm kích thước final image",
        "Tăng tốc độ build",
        "Bắt buộc phải dùng khi deploy lên AWS",
        "Cho phép chạy nhiều ứng dụng trong một container"
      ],
      "answer": 0,
      "explanation": "Multi-stage build: Stage 1 (maven:3.9.7) compile và tạo JAR. Stage 2 (openjdk:21) chỉ copy JAR từ stage 1 — final image không chứa Maven, source code, hay build tools. Kết quả: image nhỏ hơn nhiều, bảo mật hơn."
    },
    {
      "id": 16,
      "level": "advanced",
      "question": "Tại sao cache key trong actions/cache nên dựa trên hash của pom.xml?",
      "options": [
        "Để cache tự động invalidate khi dependencies thay đổi (pom.xml thay đổi → hash thay đổi → cache mới được tạo)",
        "Vì pom.xml chứa runner configuration",
        "Để tránh conflict giữa nhiều branches",
        "Đây là yêu cầu bắt buộc của GitHub Actions"
      ],
      "answer": 0,
      "explanation": "Cache key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}. Khi pom.xml thay đổi (thêm/xóa dependency), hash thay đổi → cache key thay đổi → download dependencies mới. Khi pom.xml không đổi → dùng cache cũ → build nhanh hơn."
    },
    {
      "id": 17,
      "level": "advanced",
      "question": "Trong Complete CI/CD example từ slide, workflow Integration Test được trigger bởi event nào?",
      "options": ["push lên mọi branches", "pull_request lên branch master", "schedule chạy hàng ngày", "workflow_dispatch (chạy thủ công)"],
      "answer": 1,
      "explanation": "Theo workflow trong slide: on: pull_request: branches: [master] — workflow Integration Test chạy khi có Pull Request nhắm vào branch master. Đây là pattern phổ biến để chạy tests trước khi merge."
    },
    {
      "id": 18,
      "level": "advanced",
      "question": "Để deploy lên AWS Elastic Beanstalk từ GitHub Actions, lệnh nào được dùng?",
      "options": [
        "aws deploy create-deployment",
        "eb init -p java-17 app-name --region us-west-2, sau đó eb deploy",
        "kubectl apply -f deployment.yaml",
        "docker push aws-ecr-url/app"
      ],
      "answer": 1,
      "explanation": "Deploy lên AWS Elastic Beanstalk: eb init -p java-17 spring-boot-app --region us-west-2 (khởi tạo EB environment với Java 17 platform tại region us-west-2), sau đó eb deploy để deploy version mới."
    }
  ]
}
