{
  "title": "Chương 8: Containerization — Docker & Kubernetes",
  "theory": [
    {
      "heading": "Monolithic vs Microservices Architecture",
      "content": "**Monolithic Architecture:**\n- Toàn bộ app (UI, business logic, data layer) đóng gói trong **1 WAR/JAR file**\n- Deploy trên một server (ví dụ: **Tomcat**)\n- Pros:\n  - Simple to develop, test, deploy ban đầu\n  - Dễ debug (mọi thứ trong cùng process)\n  - Less infrastructure cost\n- Cons:\n  - **Tightly coupled** — thay đổi 1 phần ảnh hưởng toàn bộ\n  - **Single bug** có thể crash toàn bộ app\n  - Redeploy **toàn bộ** app dù chỉ thay đổi nhỏ\n  - Scaling khó — phải scale toàn bộ app, không chỉ phần cần\n  - Khó adopt công nghệ mới\n\n**Microservices Architecture:**\n- Mỗi service (Account, Inventory, Shipping...) chạy **độc lập**\n- Communicate qua **REST API** hoặc message queue\n- Mỗi service có **database riêng**\n- Pros:\n  - **Decoupled** — deploy/scale độc lập từng service\n  - **Continuous delivery** — release nhanh, thường xuyên\n  - Dễ test hơn (service nhỏ hơn)\n  - Chọn tech stack phù hợp cho từng service\n- Cons:\n  - Deployment complexity tăng\n  - **'Matrix from Hell'**: nhiều services × nhiều environments → dependency hell\n  - ⚠️ NOTE: Docker sinh ra để giải quyết 'Matrix from Hell'"
    },
    {
      "heading": "Docker — Giải pháp cho 'It works on my machine'",
      "content": "**Vấn đề 'It works on my machine':**\n- Developer code trên máy mình → chạy được\n- Tester/Production có OS/dependencies khác → **crash**\n- Nguyên nhân: môi trường không nhất quán\n\n**Docker giải quyết bằng Containers:**\n- **Container** = lightweight, standalone, executable package chứa:\n  - App code\n  - Runtime (Python, Node, Java...)\n  - Libraries và dependencies\n  - Config files\n- Ví dụ: `Ubuntu Base Image + Python 3.9 + pip packages`\n- Developer và Tester dùng **cùng Docker image** → môi trường giống hệt nhau\n\n**Docker là gì:**\n- Software development tool và **virtualization technology**\n- Giúp develop, deploy, manage applications bằng containers\n- **Client-Server Architecture:**\n  - `Docker Client` — CLI mà người dùng tương tác\n  - `Docker Daemon` — background service: build, run, distribute containers\n  - Giao tiếp qua **REST API**\n  - `Docker Registry` (Docker Hub) — lưu trữ images\n- Flow: `CLIENT → (REST API) → DAEMON → REGISTRY / IMAGES / CONTAINERS`\n\n⚠️ NOTE: Docker Hub là public registry — có thể dùng private registry (AWS ECR, Azure ACR) cho production"
    },
    {
      "heading": "VMs vs Docker Containers — So sánh chi tiết",
      "content": "**Virtual Machines (VMs):**\n- **Hardware-level** process isolation\n- Mỗi VM có **separate OS** (full OS install)\n- Boot time: **vài phút**\n- Size: **vài GBs**\n- Tạo VM mới: tốn thời gian\n- Resource usage: **cao** (mỗi VM tiêu thụ RAM/CPU cho cả OS)\n\n**Docker Containers:**\n- **OS-level** process isolation\n- Containers **share host OS kernel** — không cần full OS\n- Boot time: **vài giây**\n- Size: **KBs đến MBs**\n- Pre-built images sẵn có trên Docker Hub\n- Resource usage: **thấp hơn** đáng kể\n- ⚠️ NOTE: Containers **không di chuyển** — bị destroy và re-create khi cần\n\n**Hypervisor** — phần mềm tạo và chạy VMs:\n- **Type 1 (Bare Metal):** cài trực tiếp trên **hardware**, không cần OS\n  - Ví dụ: `VMware ESXi`, `Microsoft Hyper-V`\n  - Hiệu năng cao hơn → dùng cho production servers\n- **Type 2 (Hosted):** chạy như **application trên OS** thông thường\n  - Ví dụ: `VirtualBox`, `VMware Player`\n  - Dễ cài hơn → dùng cho development/testing\n\n**So sánh nhanh:**\n- VMs tốt cho: isolation hoàn toàn, legacy apps, security cao\n- Containers tốt cho: microservices, CI/CD, scale nhanh, lightweight"
    },
    {
      "heading": "Docker Commands cơ bản",
      "content": "**Docker Commands cơ bản:**\n\n**Command format:**\n- Cú pháp: `docker <command> <sub-command>` (ví dụ: `docker image ls`, `docker container run`)\n\n**Version & Info:**\n- `docker version` — xem version của client và daemon\n- `docker info` — hiển thị **system-wide info**: số containers (running/paused/stopped), số images, server version, storage driver, network plugins\n- **Docker home directory:** `/var/lib/docker` — nơi lưu trữ tất cả images, containers, volumes, networks information\n\n**Images:**\n- `docker pull [image]` — pull image từ Docker Hub (ví dụ: `docker pull ubuntu`)\n- `docker image ls` (= `docker images`) — list images với các cột: **REPOSITORY, TAG, IMAGE ID, CREATED, SIZE**\n- `docker image --help` — hiển thị tất cả image subcommands: build, history, import, inspect, ls, prune, pull, push, rm, save, tag\n- `docker rmi [image]` — xóa image\n\n**Containers — Run:**\n- `docker run [image]` — tạo và chạy container từ image (chạy **foreground**, terminal attached — exit terminal thì container bị kill)\n- `docker container run nginx` — **các bước xảy ra:**\n  1. Docker tìm image trong **local image cache**\n  2. Nếu không có → tìm trong **remote image repository** (Docker Hub by default)\n  3. Download phiên bản mới nhất (`nginx:latest` by default)\n  4. Tạo **container mới** từ image đó\n  5. Container được gắn vào **network**, nhận **virtual IP** trong private network (default bridge network)\n  6. Mở **ports** để phục vụ requests\n  7. Khởi chạy process được chỉ định trong **CMD** của Dockerfile\n- `docker run --detach nginx` (= `docker run -d nginx`) — chạy **background** (detached mode)\n- `docker run --detach -p 8080:80 nginx` — port mapping **host:container**\n- `docker run --detach -p 8080:80 --name webhost nginx` — **named container**\n- `docker run -it [image] bash` — chạy interactive với terminal\n\n**Containers — Manage:**\n- `docker ps` — list **running** containers\n- `docker ps -a` — list **tất cả** containers (kể cả stopped)\n- `docker container stop <id>` — dừng container đang chạy\n- `docker start <id>` — khởi động lại stopped container\n- `docker container kill <id>` — kill container ngay lập tức\n- `docker container rm <name>` — xóa container\n- `docker container logs <name>` — xem logs; `docker container logs -f <name>` — **follow logs** (realtime)\n- `docker exec -it [container] bash` — vào shell trong container **đang chạy**\n- `docker top <container>` — list **processes** đang chạy trong container\n- `docker container stats <container>` — xem **CPU và memory usage** của container\n- `docker container run -it --name ubuntu bash` — interactive mode (`-i` = interactive/STD_IN, `-t` = terminal/STD_OUT)\n- `docker container inspect <id>` — xem **config và metadata** của container dưới dạng **JSON array**\n- `docker container inspect --format '{{.NetworkSettings.IPAddress}}' <id>` — lấy **IP address** của container\n\n**Cài đặt Docker:**\n- Windows 10 Pro/Enterprise: dùng **Hyper-V** với Linux VM nhỏ\n- Windows 7/8/8.1: **Docker Toolbox** — chạy Linux VM trong VirtualBox\n- Online thử nghiệm: `labs.play-with-docker.com`"
    },
    {
      "heading": "Docker Image, Container & Union File System",
      "content": "**Docker Image:**\n- **Read-only template** dùng để tạo Docker containers\n- Chứa application binaries và dependencies\n- Lưu trữ trong **Docker Registry**\n- Nhiều containers có thể được tạo từ **cùng một image**\n- Các thay đổi trong container **KHÔNG được lưu** vào image theo mặc định\n\n**Docker Container:**\n- **Running instance** của một Docker image\n- Chứa toàn bộ package cần thiết để chạy application\n- Mỗi service chạy với **dependencies riêng** trong các container riêng biệt\n\n**Union File System (UFS) & Copy-on-Write (COW):**\n- Docker image là **read-only template** — không thể sửa trực tiếp\n- Khi container cần **thay đổi một file** từ read-only image, nó **copy file đó lên layer read-write riêng** của container — đây gọi là cơ chế **Copy-on-Write (COW)**\n- Các file mới/đã sửa được **'committed' thành layer mới**\n- `docker history` — xem tất cả các layers của image\n\n**Cấu trúc layers (từ dưới lên):**\n1. **bootfs** — kernel, cgroups, namespace, device mapper\n2. **Base Image** (ví dụ: Ubuntu)\n3. Add emacs\n4. Add Apache\n5. **Writeable Container** (layer trên cùng, chỉ tồn tại khi container chạy)"
    },
    {
      "heading": "Docker Registry",
      "content": "**Docker Registry:**\n- Hệ thống **storage và distribution** cho Docker images\n- Được tổ chức thành **Docker repositories** — mỗi repository lưu tất cả versions của một image cụ thể\n- Mặc định, Docker engine tương tác với **DockerHub** (Docker's public registry instance)\n\n**Image naming:**\n- Format: `username/image-name` (ví dụ: `docker pull ubuntu:19.10` để pull tag cụ thể)\n\n**Private Registry:**\n- Có thể chạy **on-premise private repositories** (ví dụ: **Harbor**)\n\n**Cloud Provider Registries:**\n- **Amazon Elastic Container Registry (ECR)** — AWS\n- **Google Container Registry** — Google Cloud\n- **Azure Container Registry (ACR)** — Microsoft Azure"
    },
    {
      "heading": "Container Orchestration — Kubernetes & Docker Swarm",
      "content": "**Container Orchestration** tự động hóa việc quản lý containers ở scale lớn.\n\n**Các tác vụ được tự động hóa:**\n- Scheduling và deploying containers lên đúng hosts\n- Provisioning & resource allocation\n- Redundancy & availability (HA)\n- **Scaling** up/down containers theo demand\n- Moving containers khi host thiếu resources hoặc chết\n- External service exposure & **load balancing**\n- **Health monitoring** — restart containers khi fail\n\n**Docker Swarm vs Kubernetes — So sánh:**\n\n| Feature | Docker Swarm | Kubernetes |\n|---|---|---|\n| Setup | **Simple** | Complex |\n| Scaling speed | **5x faster** | Slower |\n| Auto-scaling | ❌ Không có | ✅ Có |\n| GUI | ❌ Không có | ✅ Dashboard |\n| Rollback | Thủ công | **Automatic** |\n| Logging/Monitoring | 3rd party cần | **In-built** |\n| Volume sharing | Bất kỳ container | Chỉ trong cùng **Pod** |\n\n⚠️ NOTE: Swarm đơn giản hơn nhưng Kubernetes **mạnh hơn** và được dùng rộng rãi hơn trong production"
    },
    {
      "heading": "Kubernetes (K8s) — Architecture",
      "content": "**Kubernetes (K8s)** — open-source Container Management tool\n- Viết bằng **Golang**\n- Originally developed by **Google**, donated to **CNCF**\n- Tên viết tắt: K + **8 ký tự** + s = **K8s**\n- Cung cấp: container runtime, orchestration, self-healing, service discovery, load balancing, auto-scaling\n\n**Cluster** = tập hợp machines để chạy containerized apps\n- Mỗi machine = **Node**\n- Hai loại node: **Master Node** và **Worker Node**\n\n**Master Node** (Control Plane) — quản lý cluster:\n\n1) **ETCD** — distributed key-value store\n- Lưu **toàn bộ cluster data** (state, config)\n- Implement **locks** giữa Masters để tránh conflict\n- Distributed: lưu trên tất cả master nodes\n\n2) **Scheduler** — phân phối workloads\n- Assign **newly created containers/Pods** lên nodes phù hợp\n- Dựa trên: resource requirements, node capacity, constraints\n\n3) **Controller Manager** — brain of orchestration\n- Detect nodes/containers/endpoints bị **down**\n- **Bring up new containers** để maintain desired state\n- Chạy nhiều control loops (Node Controller, Replication Controller...)\n\n4) **API Server** — entry point chính\n- Validate và thực thi **REST commands**\n- Đảm bảo `etcd` config khớp với deployed containers\n- Mọi communication trong cluster đều qua API Server\n\n**Worker Node** — chạy containerized apps:\n\n- **Kubelet** — agent trên worker node\n  - Giao tiếp với Master, nhận Pod Spec từ API Server\n  - Report health info lên Master\n  - Đảm bảo containers chạy đúng theo spec\n\n- **Kube-proxy** — network proxy\n  - Route network traffic theo **network policies**\n  - Maintain network rules trên nodes\n\n**`kubectl`** — CLI tool để interact với K8s:\n- Dùng APIs của API Server\n- `kubectl run nginx` — deploy app\n- `kubectl cluster-info` — xem cluster info\n- `kubectl get nodes` — list all nodes"
    },
    {
      "heading": "Kubernetes Pods & K3s",
      "content": "**Pod** — đơn vị cơ bản nhất trong Kubernetes:\n- **Smallest deployable unit** có thể create/schedule/manage\n- K8s **không run containers trực tiếp** — wrap chúng vào **Pod**\n- 1 Pod có thể chứa 1 hoặc nhiều containers\n- Mỗi Pod có **unique IP address**\n- Containers trong cùng Pod: share **storage volumes** + **network**, communicate qua `localhost`\n- Pods là **ephemeral** (tạm thời) — có thể bị destroy bất cứ lúc nào\n- **Recommended: 1 container per Pod** (vì pods scale as a unit)\n- `initContainer`: chạy đến khi hoàn thành trước container chính (dùng cho setup)\n\n**K3s** (by **Rancher**) — lightweight Kubernetes cho Edge:\n- **Fully compliant** Kubernetes distribution\n- Thiết kế cho: Edge devices, IoT, ARM hardware, CI environments\n- Khác biệt so với K8s tiêu chuẩn:\n  - Packaged as **single binary** (<100MB)\n  - Dùng **SQLite** thay `ETCD` (nhẹ hơn)\n  - Dùng **`containerd`** thay Docker\n  - Inbuilt **Ingress controller** (`Traefik`)\n  - Memory footprint: **<100MB**\n  - Hỗ trợ: `ARM64`, `ARMv7`, `x86_64`\n- Setup: 3 VMs (1 master + 2 workers) → install qua `curl` script → master tạo token → workers join bằng `K3S_URL` + `K3S_TOKEN`\n\n**Certified K8s Distributions:**\n- Cloud Managed: `EKS` (AWS), `AKS` (Microsoft Azure), `GKE` (Google)\n- Self Managed: `OpenShift`, `Docker Enterprise`\n- Local/Dev: `Minikube`, `MicroK8s`\n- Vanilla: `Kubeadm`"
    }
  ],
  "questions": [
    {
      "id": 1,
      "level": "basic",
      "question": "Docker Container khác Virtual Machine ở điểm gì về thời gian khởi động?",
      "options": ["Container khởi động trong vài phút; VM khởi động trong vài giây", "Container khởi động trong vài giây; VM khởi động trong vài phút", "Cả hai đều khởi động trong vài giây", "Cả hai đều khởi động trong vài phút"],
      "answer": 1,
      "explanation": "Docker Containers boot in seconds (nhẹ, chỉ cần OS-level isolation). VMs boot in minutes (cần full OS boot). Đây là một trong những lợi thế lớn nhất của containers."
    },
    {
      "id": 2,
      "level": "basic",
      "question": "Container trong Docker là gì?",
      "options": ["Một loại virtual machine nhẹ", "Lightweight, stand-alone, executable package chứa app + libraries + config + dependencies", "Một server vật lý", "Một loại hypervisor"],
      "answer": 1,
      "explanation": "Container là lightweight, stand-alone, executable package chứa tất cả những gì cần để chạy app: code, runtime, libraries, config files, dependencies. Ví dụ: Ubuntu Base Image + Python + Dependencies."
    },
    {
      "id": 3,
      "level": "basic",
      "question": "Docker sử dụng kiến trúc gì?",
      "options": ["Peer-to-peer architecture", "Client-server architecture — Docker Client giao tiếp với Docker Daemon", "Microservices architecture", "Monolithic architecture"],
      "answer": 1,
      "explanation": "Docker dùng client-server architecture: Docker Client giao tiếp với Docker Daemon qua REST API. Daemon chịu trách nhiệm build, run, distribute containers. Images được lưu trên Registry (Docker Hub)."
    },
    {
      "id": 4,
      "level": "basic",
      "question": "Kubernetes còn được gọi là gì?",
      "options": ["K9s", "K8s", "K7s", "Kube"],
      "answer": 1,
      "explanation": "Kubernetes = K8s (viết tắt: K + 8 ký tự ở giữa + s). Kubernetes là open-source Container Management tool viết bằng Golang, originally by Google, donated to CNCF."
    },
    {
      "id": 5,
      "level": "basic",
      "question": "Trong Kubernetes cluster, hai loại node là gì?",
      "options": ["Primary node và Secondary node", "Master node (control plane) và Worker node (chạy containers)", "Leader node và Follower node", "Server node và Client node"],
      "answer": 1,
      "explanation": "K8s cluster có: Master Node (hosts control plane components: ETCD, Scheduler, Controller, API Server — quản lý cluster) và Worker Nodes (chạy containerized applications)."
    },
    {
      "id": 6,
      "level": "basic",
      "question": "Pod trong Kubernetes là gì?",
      "options": ["Tên của một Docker image", "Basic scheduling unit — smallest deployable unit, wraps 1 hoặc nhiều containers, có unique IP", "Tên của một worker node", "Một loại Kubernetes service"],
      "answer": 1,
      "explanation": "Pod là basic scheduling unit và smallest deployable unit trong K8s. K8s không run containers trực tiếp mà wrap chúng vào Pod. Mỗi pod có unique IP address. Containers trong cùng pod communicate qua localhost."
    },
    {
      "id": 7,
      "level": "basic",
      "question": "Lệnh Docker nào dùng để xem danh sách tất cả containers (kể cả đã dừng)?",
      "options": ["docker list -all", "docker ps -a", "docker containers --all", "docker show all"],
      "answer": 1,
      "explanation": "'docker ps' chỉ hiện running containers. 'docker ps -a' (all) hiện tất cả containers kể cả stopped. Đây là lệnh cơ bản để debug và quản lý containers."
    },
    {
      "id": 8,
      "level": "intermediate",
      "question": "Nhược điểm chính của Monolithic Architecture là gì?",
      "options": ["Simple to develop và deploy", "Single bug có thể crash toàn bộ app, khó scale, redeploy toàn bộ khi update nhỏ, tightly coupled", "Less costly và easy debugging", "Simple to test và deploy"],
      "answer": 1,
      "explanation": "Monolithic cons: tightly coupled, single bug crashes entire app, khó scale (phải scale toàn bộ), redeploy toàn bộ app khi update dù chỉ thay đổi nhỏ, difficult to adopt new technology vì changes affect entire app."
    },
    {
      "id": 9,
      "level": "intermediate",
      "question": "Hypervisor Type 1 (bare metal) khác Type 2 (hosted) như thế nào?",
      "options": [
        "Type 1: chạy trên OS (VirtualBox); Type 2: chạy trực tiếp trên hardware",
        "Type 1: chạy trực tiếp trên hardware (VMware ESXi, Hyper-V); Type 2: chạy như software layer trên OS (VirtualBox, VMware Player)",
        "Không có sự khác biệt",
        "Type 1 chậm hơn Type 2"
      ],
      "answer": 1,
      "explanation": "Type 1 (bare metal): cài trực tiếp trên hardware, không cần OS — examples: VMware ESXi, Microsoft Hyper-V. Hiệu suất cao hơn. Type 2 (hosted): chạy như application trên OS — examples: VirtualBox, VMware Player. Dễ cài hơn."
    },
    {
      "id": 10,
      "level": "intermediate",
      "question": "Sự khác biệt về auto-scaling giữa Kubernetes và Docker Swarm là gì?",
      "options": [
        "Cả hai đều hỗ trợ auto-scaling",
        "Kubernetes có auto-scaling; Docker Swarm KHÔNG có auto-scaling",
        "Docker Swarm có auto-scaling; Kubernetes không",
        "Cả hai đều không hỗ trợ auto-scaling"
      ],
      "answer": 1,
      "explanation": "Theo bảng so sánh từ slide: Kubernetes can do auto-scaling. Docker Swarm cannot do auto-scaling. Đây là một trong những lý do K8s được ưa chuộng hơn cho production environments."
    },
    {
      "id": 11,
      "level": "intermediate",
      "question": "ETCD trong Kubernetes Master có vai trò gì?",
      "options": [
        "Chạy container runtime trên worker nodes",
        "Distributed key-value store lưu toàn bộ cluster data và implement locks giữa Masters",
        "Route network traffic trong cluster",
        "Schedule containers onto nodes"
      ],
      "answer": 1,
      "explanation": "ETCD là distributed reliable key-value store lưu toàn bộ data dùng để manage K8s cluster. Khi có nhiều master nodes, ETCD stores thông tin trên tất cả nodes theo cách distributed. Implement locks để không có conflicts giữa Masters."
    },
    {
      "id": 12,
      "level": "intermediate",
      "question": "Kubelet trên Worker Node có nhiệm vụ gì?",
      "options": [
        "Lưu trữ cluster state",
        "Giao tiếp với Master, report health info và thực thi các actions master yêu cầu",
        "Route network traffic",
        "Schedule containers"
      ],
      "answer": 1,
      "explanation": "Kubelet là agent trên worker node: interact với master để provide health information, carry out actions requested by master (start/stop containers). Kubelet nhận Pod Spec từ API Server và đảm bảo containers chạy theo spec."
    },
    {
      "id": 13,
      "level": "intermediate",
      "question": "kubectl là gì và dùng để làm gì?",
      "options": [
        "Kubernetes Dashboard UI",
        "CLI tool để interact với K8s cluster qua API Server — deploy apps, get cluster info, list nodes",
        "Container runtime trong K8s",
        "Network proxy trong worker nodes"
      ],
      "answer": 1,
      "explanation": "kubectl (kube control) là command line utility để interact với K8s cluster. Dùng APIs của API Server. Commands: kubectl run nginx (deploy app), kubectl cluster-info (view cluster info), kubectl get nodes (list nodes)."
    },
    {
      "id": 14,
      "level": "intermediate",
      "question": "Container Orchestration tự động hóa những tác vụ nào?",
      "options": [
        "Chỉ deployment và scaling",
        "Scheduling, provisioning, availability, scaling, load balancing, health monitoring, resource allocation, service exposure",
        "Chỉ health monitoring",
        "Chỉ networking giữa containers"
      ],
      "answer": 1,
      "explanation": "Container Orchestration tự động hóa: configuring/scheduling containers, provisioning/deployment, redundancy/availability, scaling up/down, moving containers giữa hosts, resource allocation, external service exposure, load balancing, health monitoring."
    },
    {
      "id": 15,
      "level": "advanced",
      "question": "K3s của Rancher khác Kubernetes tiêu chuẩn như thế nào?",
      "options": [
        "K3s không phải Kubernetes",
        "K3s: single binary, <100MB memory, SQLite thay ETCD, containerd thay Docker, inbuilt Traefik ingress — tối ưu cho Edge devices",
        "K3s chỉ chạy trên ARM",
        "K3s không hỗ trợ Pods"
      ],
      "answer": 1,
      "explanation": "K3s (by Rancher) là fully compliant K8s distribution nhưng lightweight: packaged as single binary, <100MB memory, supports ARM/x86, SQLite thay thế ETCD nặng, containerd thay Docker, inbuilt Ingress controller (Traefik). Thiết kế cho Edge devices."
    },
    {
      "id": 16,
      "level": "advanced",
      "question": "Tại sao nên giới hạn 1 container per Pod trong Kubernetes?",
      "options": [
        "Vì K8s không hỗ trợ multi-container pods",
        "Vì pods scale as a unit — nếu 2 containers trong 1 pod, chúng buộc phải scale cùng nhau dù chỉ 1 container cần scale → lãng phí resources",
        "Vì 2 containers không thể communicate trong cùng pod",
        "Vì multi-container pods không thể có IP address"
      ],
      "answer": 1,
      "explanation": "Pods are the unit of scale in K8s — tất cả containers trong pod scale cùng nhau. Nếu pod có nginx (cần scale) và mysql (không cần scale), bạn buộc phải tạo thêm cả mysql khi scale nginx → wasted resources. Best practice: 1 container per pod."
    },
    {
      "id": 17,
      "level": "advanced",
      "question": "Controller Manager trong Kubernetes làm gì khi một node bị down?",
      "options": [
        "Gửi alert email đến admin",
        "Detect node down và bring up new containers trên nodes khác để maintain desired state",
        "Xóa toàn bộ deployment",
        "Chỉ log lỗi và chờ admin xử lý"
      ],
      "answer": 1,
      "explanation": "Controller Manager là 'brain behind orchestration' — responsible for noticing và responding khi nodes, containers, endpoints go down. Chạy control loops để check if required deployments/replicas/nodes are running và bring up new containers khi cần để maintain desired state."
    },
    {
      "id": 18,
      "level": "advanced",
      "content": "Docker Swarm có ưu điểm gì hơn Kubernetes về tốc độ scaling?",
      "question": "Docker Swarm có ưu điểm gì hơn Kubernetes về tốc độ scaling?",
      "options": [
        "Swarm không hỗ trợ scaling",
        "Docker Swarm scales 5x faster than Kubernetes",
        "Swarm và Kubernetes có tốc độ scaling tương đương",
        "Kubernetes scales 5x faster than Swarm"
      ],
      "answer": 1,
      "explanation": "Theo bảng so sánh trong slide: Docker Swarm 'Highly scalable & scales 5x faster than Kubernetes'. Tuy nhiên Swarm không có auto-scaling (phải scale thủ công), trong khi Kubernetes có auto-scaling built-in."
    },
    {
      "id": 19,
      "level": "basic",
      "question": "Sự khác biệt chính giữa Docker Image và Docker Container là gì?",
      "options": [
        "Image là running instance; Container là read-only template",
        "Image là read-only template dùng để tạo containers; Container là running instance của một image",
        "Image và Container là cùng một thứ, chỉ khác tên",
        "Container là file lưu trữ trên registry; Image là process đang chạy"
      ],
      "answer": 1,
      "explanation": "Docker Image là read-only template chứa application binaries và dependencies — dùng để tạo containers, lưu trên Docker Registry. Docker Container là running instance của image — chứa toàn bộ package cần để chạy app. Nhiều containers có thể được tạo từ cùng một image, nhưng thay đổi trong container không lưu vào image theo mặc định."
    },
    {
      "id": 20,
      "level": "intermediate",
      "question": "Copy-on-Write (COW) trong Docker Union File System hoạt động như thế nào?",
      "options": [
        "Mỗi lần chạy container, Docker tạo một bản copy hoàn toàn mới của image",
        "Docker image là read-only; khi container cần thay đổi một file, nó copy file đó lên layer read-write riêng của container — chỉ ghi khi cần thay đổi",
        "Docker ghi trực tiếp vào image gốc mỗi khi container thay đổi file",
        "COW có nghĩa là tất cả containers phải dùng chung một writable layer"
      ],
      "answer": 1,
      "explanation": "Copy-on-Write (COW) là cơ chế của Docker Union File System: Image luôn là read-only. Khi container cần thay đổi một file từ image, nó KHÔNG sửa image trực tiếp — thay vào đó, nó copy file lên layer read-write riêng của container và sửa tại đó. File mới/đã sửa được committed thành layer mới. Lệnh 'docker history' hiển thị tất cả layers. Cấu trúc từ dưới lên: bootfs → Base Image → các layers thêm → Writeable Container."
    },
    {
      "id": 21,
      "level": "intermediate",
      "question": "Khi chạy lệnh 'docker container run nginx', Docker thực hiện các bước theo thứ tự nào?",
      "options": [
        "Pull image từ Docker Hub → tạo container → tìm image local → gán IP → mở ports → khởi chạy process",
        "Tìm image trong local cache → nếu không có thì pull từ Docker Hub → tạo container → gán virtual IP trong private network → mở ports → khởi chạy process theo CMD trong Dockerfile",
        "Tạo container trống → pull image → copy image vào container → gán IP → chạy app",
        "Kết nối Docker Hub → pull image → tạo container → gán IP → chạy process"
      ],
      "answer": 1,
      "explanation": "Khi chạy 'docker container run nginx', các bước theo thứ tự: 1) Tìm image 'nginx' trong local image cache; 2) Nếu không có → tìm trong remote image repository (Docker Hub by default); 3) Download nginx:latest; 4) Tạo container mới từ image; 5) Container được gắn vào network, nhận virtual IP trong private network (default bridge network); 6) Mở ports để phục vụ requests; 7) Khởi chạy process được chỉ định trong CMD của Dockerfile."
    },
    {
      "id": 22,
      "level": "basic",
      "question": "Lệnh 'docker container inspect <id>' dùng để làm gì?",
      "options": [
        "Xem danh sách tất cả containers đang chạy",
        "Xem config và metadata của container dưới dạng JSON array — bao gồm network settings, volumes, environment variables",
        "Kiểm tra trạng thái health của container",
        "Xem logs của container trong realtime"
      ],
      "answer": 1,
      "explanation": "'docker container inspect <id>' trả về config và metadata của container dưới dạng JSON array. Có thể dùng --format để lấy thông tin cụ thể, ví dụ: 'docker container inspect --format \"{{.NetworkSettings.IPAddress}}\" <id>' để lấy IP address của container."
    },
    {
      "id": 23,
      "level": "basic",
      "question": "Lệnh 'docker info' hiển thị loại thông tin nào?",
      "options": [
        "Chỉ version của Docker client và daemon",
        "Chỉ danh sách các images đang có trên máy",
        "System-wide info: số containers (running/paused/stopped), số images, server version, storage driver, logging driver, network plugins",
        "Chỉ trạng thái của Docker Swarm"
      ],
      "answer": 2,
      "explanation": "'docker info' hiển thị system-wide information về Docker installation: số containers đang running/paused/stopped, tổng số images, server version, storage driver, logging driver, swarm state, network plugins. Khác với 'docker version' chỉ cho biết version của client/daemon."
    },
    {
      "id": 24,
      "level": "basic",
      "question": "Docker lưu trữ images, containers, volumes và networks ở thư mục nào trên host?",
      "options": [
        "/etc/docker",
        "/home/docker",
        "/tmp/docker",
        "/var/lib/docker"
      ],
      "answer": 3,
      "explanation": "Docker home directory là /var/lib/docker — nơi lưu trữ tất cả data của Docker: subdirectories bao gồm containers/, image/, network/, overlay2/ (storage driver layers), volumes/. Đây là nơi Docker Daemon quản lý toàn bộ state của hệ thống."
    },
    {
      "id": 25,
      "level": "basic",
      "question": "Lệnh nào dùng để đặt tên cho container khi chạy?",
      "options": [
        "docker run --label mycontainer nginx",
        "docker run --tag mycontainer nginx",
        "docker run --name mycontainer nginx",
        "docker run --id mycontainer nginx"
      ],
      "answer": 2,
      "explanation": "Flag '--name' dùng để đặt tên cho container khi chạy: 'docker run --detach -p 8080:80 --name webhost nginx'. Nếu không đặt tên, Docker sẽ tự sinh tên ngẫu nhiên. Tên container dùng được trong các lệnh quản lý như docker stop, docker logs, docker rm."
    },
    {
      "id": 26,
      "level": "basic",
      "question": "Lệnh 'docker container logs -f <name>' dùng để làm gì?",
      "options": [
        "Xóa logs của container",
        "Xem logs một lần rồi thoát",
        "Follow logs của container trong realtime (tương tự tail -f)",
        "Lưu logs ra file"
      ],
      "answer": 2,
      "explanation": "Flag '-f' (follow) trong 'docker container logs -f <name>' làm cho lệnh hiển thị logs liên tục theo realtime, không thoát — tương tự 'tail -f'. Không có flag '-f', lệnh sẽ chỉ in logs hiện có rồi exit."
    },
    {
      "id": 27,
      "level": "basic",
      "question": "Lệnh nào dùng để xem các processes đang chạy bên trong một container?",
      "options": [
        "docker ps <container>",
        "docker container stats <container>",
        "docker inspect <container>",
        "docker top <container>"
      ],
      "answer": 3,
      "explanation": "'docker top <container>' liệt kê tất cả processes đang chạy bên trong container — tương tự lệnh 'top' trên Linux nhưng scoped cho container. Khác với 'docker container stats' hiển thị CPU/memory usage realtime."
    },
    {
      "id": 28,
      "level": "basic",
      "question": "Điều gì xảy ra khi bạn thoát terminal của một container đang chạy ở foreground mode (không có -d)?",
      "options": [
        "Container tiếp tục chạy ở background",
        "Container bị tạm dừng (paused)",
        "Container bị kill khi thoát terminal",
        "Container chuyển sang detached mode tự động"
      ],
      "answer": 2,
      "explanation": "Khi chạy 'docker run [image]' không có flag -d, container chạy ở foreground với terminal attached. Khi thoát terminal, container bị kill. Để tránh điều này, dùng 'docker run -d [image]' (detached mode) — container chạy ở background, không bị ảnh hưởng khi đóng terminal."
    },
    {
      "id": 29,
      "level": "basic",
      "question": "Harbor là gì trong hệ sinh thái Docker?",
      "options": [
        "Một loại container runtime thay thế Docker",
        "On-premise private registry — cho phép tổ chức tự host Docker registry",
        "Một công cụ orchestration như Kubernetes",
        "Tên của Docker Daemon trên Windows"
      ],
      "answer": 1,
      "explanation": "Harbor là on-premise private Docker registry — cho phép tổ chức tự host registry riêng thay vì dùng Docker Hub công khai. Ngoài Harbor, các cloud providers có registries riêng: AWS ECR, Google Container Registry, Azure ACR. Private registry phù hợp cho production khi cần bảo mật và kiểm soát."
    },
    {
      "id": 30,
      "level": "intermediate",
      "question": "Thư mục /var/lib/docker chứa những subdirectory nào liên quan đến containers và storage?",
      "options": [
        "apps/, config/, logs/, temp/",
        "containers/, image/, network/, overlay2/, volumes/",
        "bin/, lib/, sbin/, usr/",
        "data/, cache/, registry/, swarm/"
      ],
      "answer": 1,
      "explanation": "/var/lib/docker là Docker home directory, chứa: containers/ (metadata mỗi container), image/ (image data), network/ (network configs), overlay2/ (Union FS layers khi dùng overlay2 storage driver), volumes/ (persistent volumes). Đây là nơi Docker Daemon lưu toàn bộ state."
    },
    {
      "id": 31,
      "level": "intermediate",
      "question": "Lệnh 'docker image --help' liệt kê những subcommand nào?",
      "options": [
        "start, stop, restart, kill, rm",
        "run, exec, attach, commit, diff",
        "build, history, import, inspect, ls, prune, pull, push, rm, save, tag",
        "create, deploy, scale, update, rollback"
      ],
      "answer": 2,
      "explanation": "Theo theory, 'docker image --help' hiển thị tất cả image subcommands: build (tạo image từ Dockerfile), history (xem layers), import, inspect (xem metadata), ls (list images), prune (xóa unused images), pull (tải từ registry), push (đẩy lên registry), rm (xóa image), save (export ra file), tag (gán tag)."
    },
    {
      "id": 32,
      "level": "intermediate",
      "question": "Sự khác biệt giữa 'docker run -d' và 'docker run' (không có -d) là gì?",
      "options": [
        "'-d' chạy container với quyền root; không có '-d' thì không có quyền root",
        "'-d' (detached) chạy container ở background, terminal không bị block; không có '-d' thì chạy foreground, thoát terminal sẽ kill container",
        "'-d' chạy container với resource limits; không có '-d' thì không giới hạn",
        "Không có sự khác biệt, '-d' chỉ là alias"
      ],
      "answer": 1,
      "explanation": "'docker run -d' (--detach) chạy container ở background (detached mode) — terminal được trả lại ngay, container tiếp tục chạy dù thoát terminal. 'docker run' không có -d chạy container ở foreground với terminal attached — thoát terminal sẽ kill container. Ví dụ: 'docker run --detach -p 8080:80 nginx'."
    },
    {
      "id": 33,
      "level": "intermediate",
      "question": "Lệnh 'docker container stats <container>' cung cấp thông tin gì?",
      "options": [
        "Danh sách tất cả containers đang chạy trên host",
        "Logs của container theo realtime",
        "CPU và memory usage của container theo realtime",
        "Network configuration của container"
      ],
      "answer": 2,
      "explanation": "'docker container stats <container>' hiển thị CPU và memory usage của container theo realtime — tương tự 'top' nhưng cho metrics của container. Khác với 'docker top' (list processes), 'docker logs' (application logs), hoặc 'docker inspect' (metadata/config)."
    },
    {
      "id": 34,
      "level": "intermediate",
      "question": "Cấu trúc Union File System của Docker image được xếp theo thứ tự nào từ dưới lên trên?",
      "options": [
        "Writeable Container → Base Image → bootfs → application layers",
        "Base Image → bootfs → Add emacs → Add Apache → Writeable Container",
        "bootfs → Base Image → các layers thêm vào (emacs, Apache...) → Writeable Container",
        "Writeable Container → các layers → Base Image → bootfs"
      ],
      "answer": 2,
      "explanation": "Cấu trúc Union FS từ dưới lên: 1) bootfs (kernel, cgroups, namespace, device mapper) — layer nền tảng thấp nhất; 2) Base Image (ví dụ Ubuntu); 3) Các layers thêm vào (ví dụ Add emacs, Add Apache); 4) Writeable Container (layer trên cùng, chỉ tồn tại khi container đang chạy). Chỉ Writeable Container là read-write; tất cả layers bên dưới là read-only."
    },
    {
      "id": 35,
      "level": "intermediate",
      "question": "Lệnh nào cho phép lấy IP address của một container bằng cách dùng format template?",
      "options": [
        "docker container logs --format '{{.NetworkSettings.IPAddress}}' <id>",
        "docker container inspect --format '{{.NetworkSettings.IPAddress}}' <id>",
        "docker container stats --format '{{.NetworkSettings.IPAddress}}' <id>",
        "docker container ip --format '{{.NetworkSettings.IPAddress}}' <id>"
      ],
      "answer": 1,
      "explanation": "'docker container inspect --format '{{.NetworkSettings.IPAddress}}' <id>' sử dụng Go template format để extract chính xác trường IP address từ JSON output của 'docker container inspect'. Đây là cách tiện lợi để lấy thông tin cụ thể mà không cần parse toàn bộ JSON."
    },
    {
      "id": 36,
      "level": "intermediate",
      "question": "Docker image naming convention dùng format nào?",
      "options": [
        "image-name:version (ví dụ: nginx:1.0)",
        "username/image-name (ví dụ: library/ubuntu hoặc myuser/myapp)",
        "registry/namespace/image:tag (chỉ dùng cho private registry)",
        "image_name__username (dùng dấu gạch chân kép)"
      ],
      "answer": 1,
      "explanation": "Theo theory, Docker image naming format là 'username/image-name'. Ví dụ: 'docker pull ubuntu:19.10' (official image không cần username prefix), hoặc 'myuser/myapp' cho user images. Images trên Docker Hub dùng format này để tổ chức repositories theo user/organization."
    },
    {
      "id": 37,
      "level": "intermediate",
      "question": "Docker Registry được tổ chức như thế nào theo repositories?",
      "options": [
        "Mỗi image version được lưu trong một registry riêng biệt",
        "Repository lưu chỉ một version duy nhất của image",
        "Mỗi repository lưu tất cả versions của một image cụ thể; registry chứa nhiều repositories",
        "Registry và repository là cùng một khái niệm, chỉ khác tên gọi"
      ],
      "answer": 2,
      "explanation": "Docker Registry được tổ chức thành Docker repositories — mỗi repository lưu tất cả versions của một image cụ thể (ví dụ: repository 'nginx' lưu nginx:1.20, nginx:1.21, nginx:latest...). Registry chứa nhiều repositories. Mặc định, Docker engine tương tác với DockerHub (Docker's public registry instance)."
    },
    {
      "id": 38,
      "level": "advanced",
      "question": "API Server trong Kubernetes Master Node đảm nhiệm những vai trò gì?",
      "options": [
        "Chỉ expose REST endpoint để kubectl kết nối",
        "Validate và thực thi REST commands; đảm bảo etcd config khớp với deployed containers; là entry point cho mọi communication trong cluster",
        "Chỉ quản lý authentication và authorization",
        "Schedule containers lên worker nodes dựa trên resource availability"
      ],
      "answer": 1,
      "explanation": "API Server là entry point chính của K8s cluster: validate và thực thi REST commands từ kubectl/external tools; đảm bảo config trong etcd khớp với deployed containers trên cluster; mọi communication trong cluster đều phải qua API Server. Không nên nhầm với Scheduler (phân phối workloads) hay Controller Manager (xử lý failures)."
    },
    {
      "id": 39,
      "level": "advanced",
      "question": "Kube-proxy trên Worker Node có chức năng gì?",
      "options": [
        "Proxy requests từ internet vào Docker Hub",
        "Giao tiếp với Master Node để nhận Pod Spec",
        "Route network traffic theo network policies và maintain network rules trên nodes",
        "Lưu trữ cluster state thay thế ETCD khi ETCD bị lỗi"
      ],
      "answer": 2,
      "explanation": "Kube-proxy là network proxy chạy trên mỗi worker node: route network traffic theo network policies được định nghĩa cho Pods/Services; maintain network rules trên nodes để các Pods có thể communicate với nhau và với services bên ngoài. Khác với Kubelet (agent giao tiếp với Master, quản lý Pods)."
    },
    {
      "id": 40,
      "level": "advanced",
      "question": "Lệnh 'docker history <image>' cho biết thông tin gì?",
      "options": [
        "Lịch sử các lệnh docker đã chạy trên host",
        "Tất cả containers đã được tạo từ image đó",
        "Tất cả các layers của image — mỗi layer tương ứng với một instruction trong Dockerfile",
        "Lịch sử tag của image theo thời gian"
      ],
      "answer": 2,
      "explanation": "'docker history <image>' hiển thị tất cả các layers tạo nên image đó — mỗi dòng là một layer tương ứng với một instruction trong Dockerfile (FROM, RUN, COPY, ADD...) cùng với size và thời gian tạo. Đây là công cụ quan trọng để debug image size và hiểu Union File System structure."
    },
    {
      "id": 41,
      "level": "advanced",
      "question": "K3s sử dụng gì thay thế ETCD để lưu cluster state, và tại sao?",
      "options": [
        "MySQL — vì MySQL phổ biến hơn trong môi trường production",
        "SQLite — vì nhẹ hơn, phù hợp với Edge devices có tài nguyên hạn chế",
        "Redis — vì Redis có hiệu năng cao hơn ETCD",
        "PostgreSQL — vì PostgreSQL hỗ trợ SQL queries phức tạp hơn"
      ],
      "answer": 1,
      "explanation": "K3s dùng SQLite thay thế ETCD để giảm memory footprint và complexity — phù hợp với mục tiêu thiết kế của K3s cho Edge devices, IoT, ARM hardware với tài nguyên hạn chế. K3s được packaged as single binary (<100MB memory), dùng containerd thay Docker, inbuilt Traefik ingress. Fully compliant với Kubernetes API."
    },
    {
      "id": 42,
      "level": "advanced",
      "question": "Hai lệnh kubectl nào dùng để kiểm tra trạng thái cơ bản của K8s cluster?",
      "options": [
        "kubectl status và kubectl health",
        "kubectl version và kubectl nodes",
        "kubectl cluster-info và kubectl get nodes",
        "kubectl describe cluster và kubectl list nodes"
      ],
      "answer": 2,
      "explanation": "Theo theory: 'kubectl cluster-info' — xem thông tin về cluster (địa chỉ API Server, CoreDNS...); 'kubectl get nodes' — liệt kê tất cả nodes trong cluster cùng với status (Ready/NotReady), roles, và version. Đây là hai lệnh cơ bản nhất để verify cluster hoạt động bình thường."
    }
  ]
}
